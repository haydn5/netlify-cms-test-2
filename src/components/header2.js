import React from "react"
// import PropTypes from 'prop-types'

const itWorks = (props) => {
    
    return (
        <div>
            <img src={props.imageInfo} alt="This is cool" />
        </div>
    )
}

export default itWorks